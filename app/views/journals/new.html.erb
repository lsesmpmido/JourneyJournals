<h1><%= t('views.common.title_new', name: Journal.model_name.human.downcase) %></h1>

<%= render 'form', journal: @journal %>

<nav class="page-nav">
  <%= link_to t('views.common.back_index', name: i18n_pluralize(Journal.model_name.human.downcase)), journals_path %>
</nav>
